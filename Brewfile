# Native scientific and report tools used by this project.
brew "gromacs"
brew "packmol"
brew "pandoc"

# LaTeX is already available on the current Mac. Its installer is intentionally
# not declared here because the installed distribution may not be Homebrew-owned.
